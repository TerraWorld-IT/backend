# Dockerfile 은 build/libs/*.jar 하나만 필요하다.
# .git/src/docs/openapi-backend/gradle 등 중간 산출물이 빌드 컨텍스트로 전송되는 것을 막기 위해
# 기본적으로 전부 제외한 뒤 필요한 jar 만 허용한다.
# 주의: dockerignore 는 부모 디렉터리가 제외되면 그 안의 파일을 !패턴으로 되살릴 수 없으므로
# build, build/libs 를 단계별로 명시해 되살려야 한다.
*

!build
!build/libs
!build/libs/*.jar
